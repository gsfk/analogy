assign(max_proofs, 0).
assign(max_seconds, 1).

set(quiet).
formulas(sos).

all x exists y ((y * y = x) & all z (((z * y = x) -> z = y) & ((y * z  = x) -> z = y))).
all x all y all z ((x * y) * z = x * (y * z)).
all x all y ((x * y) = (y * x)).

end_of_list.

formulas(goals).

exists x all y exists z ((y * x = y) & (y * z = x)).

end_of_list.
